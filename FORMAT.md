# FORMAT

このリポジトリでスキルを書くときの書式。以後これに従う。

---

## ファイル

```
skills/<スキル名>/SKILL.md
```

- スキル名は**小文字ローマ字とハイフンのみ**（`kashikoi-shigoto`、`yasumushiki`）
- 1スキル＝1ファイル。補助ファイルを作らない
- 文字コードは **UTF-8（BOMなし）**、改行は LF
- 目安は **60〜120行**。200行を超えたら、それは2つのスキルが混ざっている

---

## 骨組み

```markdown
---
name: <ファイル名と同じ>
description: <発動条件。後述>
---

# <日本語のスキル名>

<1行で「何のための判断基準か」>

**<太字で、いちばん効く一文>**

<その一文が効かない場合の補足を1〜2行>

## 進め方
<作業の順序。3〜4項目>

## 判断の型
<###で小見出し。1項目＝3〜5行>

## やらないこと
<箇条書き。禁止事項>

## 仕上げの報告
<何を添えて終わるか>
```

`## 進め方` と `## 判断の型` は必須。残りは中身があるときだけ置く。

---

## description の書き方

**ここが8割。** 本文がどれだけ良くても、description が弱いと発動しない。

この順で書く：

1. **何をするスキルか**（1文・20〜40字）
2. **いつ使うか**（場面を具体的に列挙）
3. **ユーザーが実際に打つ言葉**（カギカッコで列挙）
4. **見落としがちな適用範囲**（「〜という言葉を使っていなくても」）

```
❌ UIデザインを改善するスキル。
✅ 休式（やすむしき）— 画面から要素を減らして中心を立たせる、引き算のUI設計手順。
   Webページ・ダッシュボード・LP・管理画面など画面を作るとき、また既存画面を直す
   ときに必ず使う。「すっきりさせたい」「ごちゃごちゃする」「見やすくして」…といった
   相談はもちろん、CSSの微調整や文言の書き換えを頼まれたときも適用する。
   ユーザーが「デザイン」という言葉を使っていなくても、画面の見た目に関わる作業なら使うこと。
```

**実際に打つ言葉を入れる**のが決め手。「レイアウト」「余白」のような単語は、それ自体がトリガーになる。

---

## 本文の書き方

**断定する。** 「〜すると良いでしょう」ではなく「〜する」。判断基準なので、迷いを書かない。

**理由を1行添える。** ルールだけだと例外に対応できない。
> 0件の選択肢を消すと、中身が入ったときに戻し忘れる。**幅を1/3にして薄くする**。

**具体例で締める。** 抽象論で終わらせない。
> 「amazonで見る」→**「amazon」**

**太字は1項目に1箇所。** 全部太字は、太字がないのと同じ。

**数値で書く。** 「少し右に」ではなく「1em」。「スマホでは改行」ではなく「600px以下で `flex:1 1 100%`」。

**他スキルへの導線を書く。** 守備範囲の外に出たら渡す。
> 仕様そのものが固まっていない → `tsumeru` で詰め切ってから戻る

---

## やらないこと

- 汎用的なベストプラクティスを書く（ネットに載っていることは書かない。**自分のやり方だけ**を書く）
- 「〜を意識する」「〜に配慮する」と書く（測れないものはルールにならない）
- 1つのスキルに複数の役割を詰める（分ける）
- 補助ファイルに逃がす（読み込まれない）
- description を短く整える（**長くていい。発動しないスキルは存在しないのと同じ**）

---

## チェック

commit 前に4つ。

1. description に**ユーザーが実際に打つ言葉**が3つ以上入っているか
2. 本文に**具体例**が3つ以上あるか
3. **数値**で書けるところを言葉で書いていないか
4. README の表に1行足したか
